# 复制为 .env（已 gitignore），serve.py 启动时读取；同名环境变量优先
GEMINI_API_KEY=
# 可选：不填则启动后从 /v1beta/models 按偏好自动挑
GEMINI_IMAGE_MODEL=
GEMINI_TEXT_MODEL=
# 可选：画像尺寸 1K（默认，16:9 约 1344×768）或 2K（更锐，慢一倍）
GEMINI_IMAGE_SIZE=
